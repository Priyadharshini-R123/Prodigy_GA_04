# Prodigy_GA_04
**Very short description:**  This Python-based project uses a Conditional GAN (pix2pix) to automatically transform one image into another (such as sketch to real image) by learning image-to-image translation using deep learning.
